# Asset inventory — scratch trial

Available in this workspace:
- `Pasted text.txt` — full build brief.
- `project-info (1).md` — factual project/content source.

Unavailable in the mounted workspace:
- All mapped folio PDFs.
- Green Grid `.fig` source.
- Mario Kart ZIP.
- Résumé file.
- Final image/video/font collections.

Implementation response:
- No source file was modified.
- No stock imagery was used.
- Every media position uses the shared `.placeholder` media treatment with project-specific accent/secondary colours.
- Placeholder labels describe intended function only (hero media, interface walkthrough, research diagram, process media, final outcome).
- Aspect ratios are fixed to avoid layout shift.
- Real media can later be swapped centrally by extending `data/projects.json` and the shared `media()` renderer.
